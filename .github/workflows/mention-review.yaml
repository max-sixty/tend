name: cd-mention-review
# Reusable workflow for handling pull_request_review_comment events
# (inline review comments on PRs).

on:
  workflow_call:
    inputs:
      bot_name:
        required: true
        type: string
      bot_id:
        required: true
        type: string
      pr_number:
        required: true
        type: number
      comment_url:
        required: true
        type: string
      comment_id:
        required: true
        type: string
      is_mention:
        required: true
        type: boolean
      repository:
        required: true
        type: string
        description: "owner/repo"
      model:
        type: string
        default: "opus"
      allowed_tools:
        type: string
        default: "Bash,Edit,Read,Write,Glob,Grep,WebSearch,WebFetch,Task,Skill"
      setup_command:
        type: string
        default: ""
      allowed_non_write_users:
        type: string
        default: "*"
      system_prompt_append:
        type: string
        default: "You are operating in a GitHub Actions CI environment. Use /cd-running-in-ci before starting work."
    secrets:
      bot_token:
        required: true
      claude_token:
        required: true

jobs:
  mention:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.bot_token }}

      - name: Install CD skills and scripts
        uses: max-sixty/continuous@main

      - name: Project setup
        if: inputs.setup_command != ''
        shell: bash
        run: ${{ inputs.setup_command }}

      - name: Check out PR branch
        run: |
          PR_STATE=$(gh pr view ${{ inputs.pr_number }} --json state --jq '.state')
          if [ "$PR_STATE" = "OPEN" ]; then
            gh pr checkout ${{ inputs.pr_number }}
          else
            echo "::warning::PR is $PR_STATE — staying on default branch"
          fi
        env:
          GH_TOKEN: ${{ secrets.bot_token }}

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          show_full_output: true
          claude_code_oauth_token: ${{ secrets.claude_token }}
          github_token: ${{ secrets.bot_token }}
          bot_id: ${{ inputs.bot_id }}
          bot_name: ${{ inputs.bot_name }}
          allowed_non_write_users: ${{ inputs.allowed_non_write_users }}
          additional_permissions: |
            actions: read
          prompt: >-
            ${{ inputs.is_mention
              && format(
                'You were mentioned in an inline review comment on PR #{0} ({1}, review comment ID {2}). Read the full PR context (description, diff, recent comments, CI status) and respond. If they''re requesting changes, make the changes, commit, and push. If they''re asking a question, answer it. Reply in the review thread using `gh api repos/{3}/pulls/{0}/comments/{2}/replies -f body="..."` — do not create a new top-level comment.',
                inputs.pr_number, inputs.comment_url, inputs.comment_id, inputs.repository
              )
              || format(
                'A user left an inline review comment on a PR where you previously participated (PR #{0}, {1}, review comment ID {2}). Read the full context (description, diff, recent comments, CI status). Only respond if the comment is directed at you, asks a question you can help with, or requests changes you can make. If the conversation is between humans and doesn''t need your input, exit silently without commenting. Note: if you are the author of the PR or issue, commenters are probably expecting the author to engage — lean toward responding. Reply in the review thread using `gh api repos/{3}/pulls/{0}/comments/{2}/replies -f body="..."` — do not create a new top-level comment.',
                inputs.pr_number, inputs.comment_url, inputs.comment_id, inputs.repository
              ) }}
          claude_args: |
            --model ${{ inputs.model }}
            --allowedTools ${{ inputs.allowed_tools }}
            --append-system-prompt "${{ inputs.system_prompt_append }}"

      - name: Upload Claude Code session logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: claude-session-logs
          path: /home/runner/.claude/projects/
          retention-days: 30
          if-no-files-found: warn
