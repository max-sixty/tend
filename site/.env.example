# Override the live-data Worker URL. Used by site/src/lib/api.ts.
# Default (when unset): https://api.tend-src.com
#
# Examples:
#   PUBLIC_WORKER_URL=http://localhost:8787              # local wrangler dev
#   PUBLIC_WORKER_URL=https://currently.tend-src.com     # legacy Worker

# PUBLIC_WORKER_URL=
